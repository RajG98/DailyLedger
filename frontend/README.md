
  # Create Dashboard in React

  This is a code bundle for Create Dashboard in React. The original project is available at https://www.figma.com/design/MQGto1qAA6C4oHiko51bPs/Create-Dashboard-in-React.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  